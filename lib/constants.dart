import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF006064); // Xanh đậm (Header, Label)
  static const Color accent = Color(0xFF4DD0E1); // Xanh sáng (Button)
  static const Color background = Color(0xFFECEFF1); // Nền xám
  static const Color border = Colors.grey;
}
